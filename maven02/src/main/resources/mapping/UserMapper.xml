<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.javen.dao.RegisterDao" >
    <select id="register" parameterType="com.javen.model.User">
        insert into login (username,password,type,petname,email) values
        (#{username},#{password},#{type},#{petname},#{email});
    </select>

    <select id="login" parameterType="com.javen.model.User">

    </select>

    <!--<select id="selectAll" resultType="com.javen.model.User">
        select * from user;
    </select>

    <select id="selectById" resultType="com.javen.model.User" parameterType="java.lang.Integer">
        select  * from user where id = #{id};
    </select>

    <delete id="delete" parameterType="java.lang.Integer">
        delete from user where id=#{id};
    </delete>

    <insert id="insert" parameterType="com.javen.model.User">
        insert into user (userName,password) value
        (#{userName},#{password});
    </insert>

    <update id="update" parameterType="com.javen.model.User">
        update user set userName=#{userName} , password=#{password} where id=#{id}
    </update>

    <select id="login" resultType="com.javen.model.User" parameterType="com.javen.model.User">
        select * from user where userName=#{userName} and password=#{password}
    </select>

    <select id="Plogin" resultType="com.javen.model.User" parameterType="java.lang.String">
        select * from user where userName = #{userName} and password = #{password}
    </select>

    <select id="Clogin" resultType="com.javen.model.User" parameterType="java.lang.String">
        select * from user where userName = #{0} and password = #{1}
    </select>

    <select id="GetCount" resultType="java.lang.Integer">
        select count(*) from user
    </select>

    <select id="findAll" resultType="com.javen.model.User"  parameterType="java.lang.Integer">
        select * from user limit #{param1},#{param2}
    </select>
-->
</mapper>